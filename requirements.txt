fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-multipart>=0.0.9

# Token counting — exact tokenization for OpenAI-compatible token budgets
# Falls back to heuristic (chars ÷ 4) if not installed
tiktoken>=0.7.0

# Static file serving (included in fastapi[standard] but listed explicitly)
aiofiles>=23.2.1

# Optional — for production metrics
# prometheus-fastapi-instrumentator>=7.0.0

# Dev / testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0     # test client
